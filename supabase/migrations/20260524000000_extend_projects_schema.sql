-- The columns (project_name, project_code, down_payment_percent, amenities, nearby_locations, internal_notes) 
-- already exist in your table. To force Supabase to refresh its API schema cache and see these columns, 
-- run the following command in the SQL Editor:

NOTIFY pgrst, 'reload schema';
